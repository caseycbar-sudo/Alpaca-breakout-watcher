name: Test alert delivery

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/test-alert.yml"

permissions:
  contents: read

jobs:
  test-alert:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Send harmless bridge healthcheck
        env:
          ALERT_EMAIL_FROM: ${{ secrets.ALERT_EMAIL_FROM }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          python - <<'PY'
          import os
          from email.message import EmailMessage
          import smtplib

          sender = os.environ["ALERT_EMAIL_FROM"].strip()
          recipient = os.environ["ALERT_EMAIL_TO"].strip()
          password = "".join(os.environ["GMAIL_APP_PASSWORD"].split())

          if sender.lower() != "caseycbarai@gmail.com":
              raise RuntimeError("ALERT_EMAIL_FROM must be caseycbarai@gmail.com")
          if len(password) != 16:
              raise RuntimeError("GMAIL_APP_PASSWORD must be Google's 16-character App Password")

          message = EmailMessage()
          message["From"] = sender
          message["To"] = recipient
          message["Subject"] = "ALPACA WATCHER HEALTHCHECK — PAPER ONLY"
          message.set_content(
              "Bridge healthcheck generated by the Alpaca Breakout Watcher repository.\n\n"
              "Retrieve fresh read-only SPY quotes and confirm the bridge reached verification.\n"
              "PAPER—NO REAL ORDER. Do not create or modify any paper-trading ledger entry."
          )

          with smtplib.SMTP_SSL("smtp.gmail.com", 465, timeout=30) as smtp:
              smtp.login(sender, password)
              smtp.send_message(message)

          print("Bridge healthcheck sent successfully.")
          PY
